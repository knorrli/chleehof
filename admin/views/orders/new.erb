<h2>Neue Rechnung</h2>
<div class="tabs-content">
  <% form_for :order, url(:orders, :create), id: 'order-form', class: 'form-horizontal', autocomplete: 'off' do |f| %>
    <%= partial 'orders/form', locals: { f: f } %>
 <% end %>
</div>
