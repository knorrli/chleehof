<h1>Kategorie bearbeiten</h1>
<% form_for :category, url(:categories, :update, id: @category.id), method: :put, class: 'form-horizontal' do |f| %>
  <div class="row">
    <div class="col-md-4">
      <%= partial 'categories/form', :locals => { :f => f } %>
    </div>
  </div>
<% end %>

