<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
    <meta content="text/html; charset=utf-8" http-equiv="Content-Type" />
    <meta name=viewport content='width=device-width,initial-scale=1' />
    <title><%= @title.present? ? "#{@title} | Chleehof vom Hof" : "Chleehof vom Hof" %></title>
    <%= favicon_tag "favicon.ico" %>
    <link href='https://fonts.googleapis.com/css?family=Varela' rel=stylesheet />
    <%= stylesheet_link_tag 'application' %>
  </head>
  <body>
    <div class="navbar">
      <div class=navbar-inner>
        <div class=container>
          <%= link_to 'Neue Rechnung', url(:orders, :new), :class => 'navbar-brand' %>
          <ul class="nav navbar-nav pull-right">
            <li>
              <%= link_to "Einstellungen", url_for(:accounts, :edit, id: current_account.id) %>
            </li>
          </ul>

          <ul class="nav navbar-nav pull-left">
            <li class="navbar-module">
              <%= link_to "Produkte", url(:products, :index) %>
            </li>
            <li class="navbar-module">
              <%= link_to "Kunden", url(:customers, :index) %>
            </li>
            <li class="navbar-module">
              <%= link_to "Rechnungen", url(:orders, :index) %>
            </li>
            <li class="navbar-module">
              <%= link_to "Abrechnungen", url(:accounting, :index) %>
            </li>
            <li class="navbar-module">
              <%= link_to "Inventar", url(:inventory, :index) %>
            </li>
            <li class="navbar-module">
              <%= link_to "Preise", url(:product_prices, :index) %>
            </li>
          </ul>
        </div>
      </div>
    </div>

    <div class='container main'>
      <div class='main-wrapper'>
        <%= {:error => 'danger', :warning => 'warning', :success => 'success', :notice => 'info'}.map { |type, class_name| flash_tag(type, :class => "alert alert-#{class_name} fade in", :bootstrap => true) }.join.html_safe %>
        <div class='row'><%= yield %></div>
        <div class='main-wrapper-push'></div>
      </div>
    </div>

    <footer class="footer"></footer>
    <%= javascript_include_tag 'jquery-3.1.1.min', 'bootstrap/bootstrap.min', :application, :products, :orders, :customers, :order_customer_search, :order_product_search, :order_form %>
  </body>
</html>
